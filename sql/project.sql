CREATE DATABASE magrathea;

CREATE TABLE events(event_name VARCHAR(40), 
name VARCHAR(40, 
category VARCHAR(40), 
time TIME, 
description TEXT, 
location POINT, 
phone CHAR(10), 
email VARCHAR(40), 
date DATE, 
PRIMARY KEY (event_name);

